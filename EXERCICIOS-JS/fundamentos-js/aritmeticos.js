const[a, b, c, d] =[3, 5, 8, 9] // Maneira de criar constante com vários valores usando Destruturing
const soma = a + b + c + d
const subtração = a -c -b
const multipli = a * b * d
const divisão = d / a
const modulo = a % 2

console.log(soma, subtração, multipli, divisão, modulo) // colocando sinal de - deixa o número negativo.